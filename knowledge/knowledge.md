# Knowledge Base

System-specific context and few-shot examples injected into the Intent
Parser's prompt (Section 4.2, Module 1). Used mainly to help the model
recognize out-of-scope requests correctly (Section 7.2 identified this as
the metric with the widest variance across models).

**Status:** placeholder — content to be written alongside `intent_parser.py`
(Build Order Step 5), once the initial capability set and few-shot examples
from `capabilities.json` are in place to draw from.
