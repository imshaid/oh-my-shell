# Copy this file to .env and fill in values. .env is gitignored — never commit real keys.

# Google AI Studio API key — only needed if using the opt-in cloud fallback
# provider (Section 7.8). Not required for the default local Ollama path.
GOOGLE_AI_STUDIO_API_KEY=