# Copy this file to .env and fill in values. .env is gitignored — never commit real keys.

# Google AI Studio API key — required. Get one free at
# https://aistudio.google.com/apikey
GOOGLE_AI_STUDIO_API_KEY=